# Base URL of Fluvio API server (leave blank to use the same origin)
VITE_API_BASE_URL=http://localhost:8080

# Set to true to bypass backend APIs and use local simulated mock data and SSE stream
VITE_USE_MOCK=false
