# Base URL of Fluvio API server (leave blank to use the same origin)
VITE_API_BASE_URL=http://localhost:8080

# Optional basic auth for secured fluviui.Handler mounts (REST only; see README for SSE caveat)
# VITE_API_BASIC_AUTH_USER=admin
# VITE_API_BASIC_AUTH_PASSWORD=secret
